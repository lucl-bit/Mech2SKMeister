"""Graphical user interface layer.

The first runnable GUI uses tkinter because it is available with Python on this
Mac. The mechanics and game modules are independent from this choice, so the
widgets can later be replaced by PySide6 without touching the solver.
"""
